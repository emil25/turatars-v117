export function App() {
  return (
    <main className="page-shell">
      <section className="starter-card" aria-labelledby="starter-title">
        <p className="eyebrow">QW Pages · Static</p>
        <h1 id="starter-title">Build the requested page here.</h1>
        <p>
          Replace this minimal starter with the real product. The production
          build will be emitted to <code>dist/</code> for static publishing.
        </p>
      </section>
    </main>
  );
}
